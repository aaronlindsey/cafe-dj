{
  "name": "coffee-thing",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:local": "wrangler d1 execute coffee-thing-db --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute coffee-thing-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.10",
    "nanoid": "^5.0.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251101.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.86.1"
  }
}
